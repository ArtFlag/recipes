diff --git a/lib/server/index.js b/lib/server/index.js
index 5a580245275e2552d2d1249b67db5d6c8bf08135..d24d97952b7b24b04ec7ff535806a916ca632e41 100644
--- a/lib/server/index.js
+++ b/lib/server/index.js
@@ -215,8 +215,9 @@ export const tokenize = (input) => lunr.tokenizer(input)
                         const docsTagsPath = trimLeadingSlash(trimTrailingSlash(docsPlugin.options.tagsBasePath));
                         if (urlMatchesPrefix(route, docsBasePath)) {
                             if (urlMatchesPrefix(route, trimLeadingSlash(`${docsBasePath}/${docsTagsPath}`)) ||
-                                urlMatchesPrefix(route, trimLeadingSlash(`${docsBasePath}/__docusaurus`))) {
-                                // Do not index tags filter pages and pages generated by the debug plugin
+                                urlMatchesPrefix(route, trimLeadingSlash(`${docsBasePath}/__docusaurus`)) ||
+                                urlMatchesPrefix(route, trimLeadingSlash(`${docsBasePath}/category`))) {
+                                // Do not index tags filter pages, pages generated by the debug plugin, or category index pages
                                 return [];
                             }
                             return {
